{
  "name": "cairn-test",
  "version": "1.0.0",
  "description": "Test suite for Hedera smart contracts",
  "scripts": {
    "test": "ts-node hederaContractTest.ts",
    "test:hardhat": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "ts-node deployContracts.ts",
    "deploy:hardhat": "hardhat run scripts/deploy.ts --network hedera-testnet",
    "verify": "hardhat verify --network hedera-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.20.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "ethers": "^6.10.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
